<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.unicar">

    <uses-permission android:name="android.permission.INTERNET"/>
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ResetPasswordActivity"></activity>
        <activity android:name=".MessageToUserActivity" />
        <activity android:name=".AcceptValueActivity" />
        <activity android:name=".WantChargeActivity" />
        <activity android:name=".HowManyUsersActivity" />
        <activity android:name=".UserChooseHourActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".activities.SelectDateActivity" />
        <activity android:name=".activities.CancelSelectedRideActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.RatingActivity" />
        <activity android:name=".activities.ReserveSelectedRide" />
        <activity android:name=".activities.SeeProfileActivity" />
        <activity android:name=".activities.ShowTravelsSelectedWayActivity" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>